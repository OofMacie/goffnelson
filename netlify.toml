[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Redirect all routes to index (Astro handles routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
